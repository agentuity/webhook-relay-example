{
  "name": "example-app",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"wrangler dev\" \"tsx scripts/webhook-relay-client.ts\"",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "concurrently": "^8.2.0",
    "tsx": "^4.7.0",
    "typescript": "^5.0.0",
    "reconnecting-websocket": "^4.4.0",
    "wrangler": "^4.4.0",
    "dotenv": "^16.3.1"
  }
}